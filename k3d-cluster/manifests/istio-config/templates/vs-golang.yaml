---
apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: golang-routes
  namespace: {{ .Values.general.namespace }}
spec:
  hosts:
  - "{{ .Values.go_api.service.name }}"
  - "{{ .Values.csharp_ui.service.name }}"
  http:
  - name: "golang-api-routes"
    match:
    - uri:
        prefix: "/quicksort"
    route:
    - destination:
        host: "{{ .Values.go_api.service.name }}.{{ .Values.general.namespace }}.svc.cluster.local"
        subset: v1
    corsPolicy:
      allowOrigins:
      - prefix: "http://{{ .Values.csharp_ui.service.name }}.localhost"
      - prefix: "http://{{ .Values.go_api.service.name }}.localhost"
      - prefix: "http://localhost"
      allowMethods:
      - GET
      - POST
      - OPTIONS
---
apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: golang-destinations
  namespace: {{ .Values.general.namespace }}
spec:
  host: "{{ .Values.go_api.service.name }}.{{ .Values.general.namespace }}.svc.cluster.local"
  subsets:
  - name: v1
    labels:
      version: v1