apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.python_flask_ui.service.name }}"
  namespace: "{{ .Values.general.namespace }}"
  labels:
    istio.io/use-waypoint: "{{ .Values.python_flask_ui.service.name }}-svc-waypoint"
spec:
  ports:
  - name: http
    port: !!int "{{ .Values.python_flask_ui.service.port }}"
    protocol: TCP
    targetPort: !!int "{{ .Values.python_flask_ui.service.targetPort }}"
  selector:
    app: "{{ .Values.python_flask_ui.name }}"
status:
  loadBalancer: {}