apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.go_api.service.name }}"
  namespace: "{{ .Values.general.namespace }}"
  labels:
    istio.io/use-waypoint: "{{ .Values.go_api.service.name }}-svc-waypoint"
spec:
  ports:
  - name: http
    port: !!int "{{ .Values.go_api.service.port }}"
    protocol: TCP
    targetPort: !!int "{{ .Values.go_api.service.targetPort }}"
  selector:
    app: "{{ .Values.go_api.name }}"
status:
  loadBalancer: {}
---
apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: "{{ .Values.go_api.service.name }}-allow"
  namespace: "{{ .Values.general.namespace }}"
spec:
  selector:
    matchLabels:
      app: "{{ .Values.go_api.name }}"
  action: ALLOW
  rules:
  - to:
    - operation:
        ports:
        - "{{ .Values.go_api.service.port }}"