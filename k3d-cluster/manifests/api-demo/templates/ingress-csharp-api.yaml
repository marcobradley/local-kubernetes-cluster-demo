apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: "{{ .Values.csharp_api.name }}-ingress"
  namespace: "{{ .Values.csharp_api.namespace }}"
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  ingressClassName: traefik
  rules:
    - host: "{{ .Values.csharp_api.name }}.localhost"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: "{{ .Values.csharp_api.service.name }}"
                port:
                  number: !!int "{{ .Values.csharp_api.service.port }}"