apiVersion: apps/v1
kind: Deployment
metadata:
  name: "{{ .Values.ollama_api.name }}-deployment"
  namespace: "{{ .Values.general.namespace }}"
spec:
  replicas: !!int "{{ .Values.ollama_api.replicaCount }}"
  selector:
    matchLabels:
      app: "{{ .Values.ollama_api.name }}"
  strategy: {}
  template:
    metadata:
      labels:
        app: "{{ .Values.ollama_api.name }}"
    spec:
      containers:
      - image: "{{ .Values.ollama_api.image.repository }}:{{ .Values.ollama_api.image.tag }}"
        name: "{{ .Values.ollama_api.name }}"
        ports:
        - containerPort: !!int "{{ .Values.ollama_api.service.targetPort }}"
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "64Mi"
            cpu: "100m"
