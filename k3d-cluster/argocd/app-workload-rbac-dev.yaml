---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: 'app-workload-rbac-dev'
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io/background
spec:
  destination:
    namespace: 'dev'
    server: 'https://kubernetes.default.svc'
  source:
    helm:
      valueFiles:
        - values.yaml
        - values-dev.yaml
    path: 'k3d-cluster/charts/workload-rbac'
    repoURL: 'https://github.com/marcobradley/local-kubernetes-cluster-demo.git'
    targetRevision: 'main'
  project: 'default'
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
