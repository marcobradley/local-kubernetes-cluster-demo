---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: 'argocd-core-k3d'
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io/background
spec:
  destination:
    namespace: 'argocd'
    server: 'https://kubernetes.default.svc'
  source:
    helm:
      valueFiles:
        - values.yaml
    path: 'k3d-cluster/manifests/argocd-core'
    repoURL: 'https://github.com/marcobradley/local-kubernetes-cluster-demo.git'
    targetRevision: "v4.0.2"
  project: 'default'
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
